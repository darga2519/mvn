pipeline {
    agent any
    tools { 
        maven 'Maven3.6' 
        jdk 'jdk1.8' 
    }
    stage ('Build') {
            steps {
	      bat 'mvn test'
                echo 'This is a minimal pipeline.'
            }
        }
}
